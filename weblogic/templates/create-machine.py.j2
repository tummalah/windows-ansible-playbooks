ADMIN_SERVER_URL = 't3://' + '{{ server_hostname }}' + ':' + '{{ admin_server_port }}';

connect('{{ weblogic_admin }}', '{{ weblogic_admin_pass }}', ADMIN_SERVER_URL);

edit();
startEdit();

cd('/')
cmo.createMachine('{{ item }}')

cd('/Machines/' + '{{ item }}' + '/NodeManager/' + '{{ item }}')
cmo.setListenAddress('{{ node_manager_listen_address }}')
disconnect();